apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smartcontract.fullname" . }}-test-connection"
  labels:
    {{- include "smartcontract.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smartcontract.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
